// Shared MCP tool definitions (names, schemas, descriptions) and agent
// authoring-docs source — the single source of truth for the API's /mcp server
// and any future in-process AI-assist plugin (D13). Populated in User Story 2.

export {};
