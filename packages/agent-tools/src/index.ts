// Shared MCP tool definitions (names, schemas, descriptions) and agent
// authoring-docs source — the single source of truth for the API's /mcp
// server and any future in-process AI-assist plugin (D13).

export * from "./tools.js";
export * from "./docs/index.js";
