node_modules/
dist/
build/
coverage/
*.log
.env
.env.local

# run outputs + instance key (data-model.md volume layout) — never committed.
# Root-only: packages/engine/data/ (name-dictionary packs) is source data and
# must stay tracked.
/data/

# editor/OS cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# vite
apps/web/.vite/

# Playwright e2e suite outputs — regenerated per run, never committed.
apps/e2e/playwright-report/
apps/e2e/test-results/
apps/e2e/blob-report/
apps/e2e/.budget-start.json
apps/e2e/compose-logs.txt
